extends layout

block content
  h1= title
  p Assignment 10 By: #{title}
  p My name is Seth Bennett
  button(type="button", class="btn btn-secondary", onclick="update()") Press Me

  div(id="target" style="margin-top: 10px") Display 
  
  script.
    var client_count = 0
    function update(){
      client_count +=1
      document.getElementById("target").innerHTML = "Value now " + client_count
    }
  a(href="/job") A Link to the Job class Search